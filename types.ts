export interface NavItem {
  id: string;
  label: string;
}

export interface Persona {
  title: string;
  role: string;
  description: string;
}

export interface MetricData {
  name: string;
  value: number;
}